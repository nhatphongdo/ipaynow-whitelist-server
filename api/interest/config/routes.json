{
  "routes": [
    {
      "method": "POST",
      "path": "/interests/claim",
      "handler": "Interest.claim",
      "config": {
        "policies": ["plugins.users-permissions.isAuthenticated"]
      }
    }
  ]
}
