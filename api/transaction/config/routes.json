{
  "routes": [
    {
      "method": "POST",
      "path": "/transactions/send",
      "handler": "Transaction.send",
      "config": {
        "policies": ["plugins.users-permissions.isAuthenticated"]
      }
    }
  ]
}
